apiVersion: v1
kind: Service
metadata:
  name: flag-service
  namespace: togglemaster
  labels:
    app.kubernetes.io/name: flag-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: flag-service
  ports:
  - name: http
    port: 8002
    targetPort: http
