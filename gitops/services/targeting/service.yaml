apiVersion: v1
kind: Service
metadata:
  name: targeting-service
  namespace: togglemaster
  labels:
    app.kubernetes.io/name: targeting-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: targeting-service
  ports:
  - name: http
    port: 8003
    targetPort: http
